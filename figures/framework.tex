% Framework figure for PSSA-VLA — TikZ source, LaTeX-renderable standalone.
% Compile with:   pdflatex framework.tex
\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes,fit,calc,backgrounds}

\definecolor{cVis}{RGB}{56,103,214}
\definecolor{cLang}{RGB}{36,128,68}
\definecolor{cPSE}{RGB}{210,90,40}
\definecolor{cAct}{RGB}{120,60,160}
\definecolor{cCRED}{RGB}{200,40,80}

\begin{document}
\begin{tikzpicture}[
    node distance=8mm and 12mm,
    every node/.style={font=\small},
    box/.style={draw,rounded corners=2pt,thick,minimum width=24mm,
                minimum height=8mm,align=center,fill=white},
    bigbox/.style={draw,rounded corners=3pt,thick,minimum width=44mm,
                   minimum height=10mm,align=center,fill=white},
    arrow/.style={-Latex,thick},
    dashedarrow/.style={-Latex,thick,dashed},
]

% --- Inputs ---
\node[box,fill=cVis!12]   (rgb)   at (0,0)        {RGB stream\\$o_t$};
\node[box,fill=cLang!12,below=of rgb] (lang) {Instruction $\ell$};
\node[box,fill=cVis!12,below=of lang] (depth) {Depth $d_t$};

% --- PSE-Tok ---
\node[bigbox,fill=cPSE!12,right=of rgb] (sam) {SAM-2\\masks @ $t<T_0$};
\node[bigbox,fill=cPSE!18,right=of sam] (pgs) {Persistent Gaussian Splat\\(POGS-style)};
\node[bigbox,fill=cPSE!25,right=of pgs] (psetok) {PSE entity tokens\\$\{e^i_t\}_{i=1}^{N}$};
\node[draw,thick,dashed,rounded corners,fit=(sam)(pgs)(psetok),
      label={[font=\small,anchor=west]above left:PSE-Tok}] (psebox) {};

% --- VLM grounding (replaced) ---
\node[bigbox,fill=cVis!12,below=10mm of pgs] (vit) {ViT frame tokens};

% --- Action head ---
\node[bigbox,fill=cAct!18,right=14mm of psetok] (head) {VLA action head};
\node[box,fill=cAct!28,right=of head] (act) {$a_t$};

% --- CRED ---
\node[bigbox,fill=cCRED!15,below=14mm of head] (cred) {CRED\\residual gate};
\node[box,fill=cCRED!28,below=of cred] (replan) {Freeze + replan};

% Edges
\draw[arrow] (rgb) -- (sam);
\draw[arrow] (rgb.east) to[bend right=18] (psetok.west |- vit.north);
\draw[arrow] (depth.east) to[bend right=24] (sam.south);
\draw[arrow] (sam) -- (pgs);
\draw[arrow] (pgs) -- (psetok);
\draw[arrow] (psetok) -- (head);
\draw[arrow] (vit) -| ($(head.south west)+(0,-1mm)$);
\draw[arrow] (lang.east) -| ($(head.south west)+(2mm,-1mm)$);
\draw[arrow] (head) -- (act);

% Cross-time consistency loop
\draw[dashedarrow] (psetok.south) |- node[pos=0.75,below,font=\scriptsize]{$\hat f^i_{t+1}=f^i_t+\Delta(a_t)$} (cred.east);
\draw[dashedarrow] (act.south) |- (cred.east);
\draw[arrow] (cred) -- (replan);
\draw[dashedarrow,red!70!black] (cred.west) -| ($(head.south)+(-2mm,-2mm)$);

% Title labels
\node[font=\small\bfseries,above=2mm of psebox] {1. Persistent scene-entity grounding};
\node[font=\small\bfseries,above=2mm of head]   {2. VLA action head with PSE prefix};
\node[font=\small\bfseries,below=2mm of replan] {3. Cross-time consistency $\Rightarrow$ error correction};

\end{tikzpicture}
\end{document}
